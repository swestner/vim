<!--http://docs.octopusdeploy.com/display/OD/Variable+Substitution+Syntax -->
<CCConfig
	xmlns:xdt="http://schemas.microsoft.com/XML-Document-Transform"
	xdt:Transform="SetAttributes(redisServerName,redisServerPort,redisStatus,elmahDbServer,Cams3DbServer,fileStorageLocal,fileStorageAzure,keenioProjectId,keenioWriteKey,mandrillApiKey,cacheExpiryTime,longerCacheExpiryTime,fileUploadFolderLocalVirtual,apiPath,sessionExpirationMinutes ,defaultProfilePhoto,defaultCoverImage,profilePhotoCoverImageAcceptedFileTypes,fileStorageAzureAvatarCoverImageFolder,translationFilesFolder,contentFolder,forgotPasswordTokenExpirationHours)"
	
	redisServerName="#{redisServerName}"
	redisServerPort="#{redisServerPort}"
	redisStatus="true"
	elmahDbServer="#{elmahDbServer}"
	Cams3DbServer="#{Cams3DbServer}"
	fileStorageLocal="#{fileStorageLocal}"
	fileStorageAzure="#{fileStorageAzure}"
	keenioProjectId="#{keenioProjectId}"
	keenioWriteKey="#{keenioWriteKey}"
	mandrillApiKey="#{mandrillKey}"
	cacheExpiryTime="#{if cacheExpiryTime}#{cacheExpiryTime}#{/if}#{unless cacheExpiryTime}2#{/unless}"
	longerCacheExpiryTime="#{if longerCacheExpiryTime}#{longerCacheExpiryTime}#{/if}#{unless longerCacheExpiryTime}5#{/unless}"
	fileUploadFolderLocalVirtual="#{if fileUploadFolderLocalVirtual}#{fileUploadFolderLocalVirtual}#{/if}#{unless fileUploadFolderLocalVirtual}uploads#{/unless}"
	apiPath="#{if apiPath}#{apiPath}#{/if}#{unless apiPath}/api/#{/unless}"
	sessionExpirationMinutes ="#{if sessionExpirationMinutes}#{sessionExpirationMinutes}#{/if}#{unless sessionExpirationMinutes}480#{/unless}"
	defaultProfilePhoto="#{if defaultProfilePhoto}#{defaultProfilePhoto}#{/if}#{unless defaultProfilePhoto}images/default-avatar.png#{/unless}"
	defaultCoverImage="#{if defaultCoverImage}#{defaultCoverImage}#{/if}#{unless defaultCoverImage}images/default-cover-1.jpg#{/unless}"
	profilePhotoCoverImageAcceptedFileTypes="#{if profilePhotoCoverImageAcceptedFileTypes}#{profilePhotoCoverImageAcceptedFileTypes}#{/if}#{unless profilePhotoCoverImageAcceptedFileTypes}jpg,jpeg,png,gif,bmp#{/unless}"
	fileStorageAzureAvatarCoverImageFolder="#{if fileStorageAzureAvatarCoverImageFolder}#{fileStorageAzureAvatarCoverImageFolder}#{/if}#{unless fileStorageAzureAvatarCoverImageFolder}avatars#{/unless}"
	translationFilesFolder="#{if translationFilesFolder}#{translationFilesFolder}#{/if}#{unless translationFilesFolder}translations#{/unless}"
	contentFolder="#{if contentFolder}#{contentFolder}#{/if}#{unless contentFolder}content#{/unless}"
	forgotPasswordTokenExpirationHours="#{if forgotPasswordTokenExpirationHours}#{/if}#{unless forgotPasswordTokenExpirationHours}24#{/unless}"
/>
